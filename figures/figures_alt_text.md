# Alt text for figures

- model_counts.png: Bar chart showing the number of responses per model. Bars: Gemini 2.5 Pro (~94), Claude Sonnet 4 (~84), GPT-5 (~41).
- model_trial_counts.png: Stacked bar chart showing counts per model, segmented by trial_type (e.g., silly_first, serious_first, tech_first, unknown). Legend identifies trial types; each model's bar is segmented accordingly.
- similarity_network.png: Network graph where each node is a (model, probe) aggregate; edges connect highly similar text clusters by TF-IDF cosine similarity. Node colors: GPT-5 (indigo), Claude Sonnet 4 (orange), Gemini 2.5 Pro (teal).

